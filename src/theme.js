// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6a91e6',  // Replace this with your custom color
    },
    secondary: {
      main: '#2663eb',  // Optional: Customize secondary color
    },
  },
});

export default theme;
